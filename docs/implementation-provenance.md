# Implementation Provenance

This document separates human design requirements, Agent implementation choices, and external/reference material used while building meteor.

## Full-Size Coverage Update (2026-09-24)

- **Human design:** inspect existing kernels and define a sufficiently broad full-size range.
- **Agent choices:** 192 finite shapes spanning M=1–8192, N=1–32769, K=1–8192; route/alignment boundaries, representative large matrices and selected historical cases; a 256 MiB pinned-data ceiling for the current single-JSON SSH transport. Preserve existing suites and receipts. Generate CPU references asynchronously with atomic per-case publication and cancellation cleanup; use a mathematically bounded FP64 integer dot product followed by the existing FP32 stages.
- **References:** local v235 kernel guards/catalog, NVIDIA CUTLASS profiler's explicit problem-size lists, and NumPy 2.3 matmul's BLAS capability. The numerical exactness derivation and matrix limits are project decisions; no vendor implementation was copied. References, source hashes and verification limits are in the [coverage audit](2026-09-24-full-size-coverage.md) and [policy](../templates/project/asc/full-size-policy.md).
- **Evidence boundary:** old four-case NPU results retain their old scope. Local oracle generation and regression success do not establish any candidate's performance or correctness on the 192-case suite.

## Human Design

- Plugin name: `meteor`.
- Target host: DeepSeek Harness.
- After requesting an upgrade and interface verification against current official documentation, the user explicitly selected **DSH 0.1.7-alpha.2**. The earlier installed 0.1.0-rc.6 is the previous compatibility target.
- Chief manages user interaction, configuration, repository maintenance, and how many research tasks to start.
- The user requested that a short UI goal be enough: existing skills carry chief's routine initialization, configuration, startup and result-collection duties. Chief initializes a missing project itself, prepares the real device and reads its hardware report, starts promptly once setup is ready, and may use a small set of internal evidence and official vendor web sources to formulate a hypothesis.
- On 2026-09-24, the user replaced the earlier default-mock/placeholder-device plan: initialization must leave an unconfigured project until chief connects the centralized profile, debugs the real device and generates a hardware report. No assumed chip, memory size, core count or compiler architecture may stand in for observed hardware facts.
- A real kernel `PASS` needs correct output and matching target-device execution evidence. Placeholder device code with Host CPU/NEON computation is not an Ascend AI Core implementation. The authoring subagent retains responsibility for its own exact-revision full-size tests; chief's hardware preparation does not take over those tests.
- The user requested official guidance for performance monitoring and test methodology in the existing skills. Hypothesis verdict remains independent of performance rank, and evidence selection follows the actual claim rather than a mandatory profiler metric for every hypothesis.
- For an explicitly authorized sustained goal, chief first verifies a stable complete research round, then chooses concurrency within overall and per-research budgets and device capacity. It may start the next distinct research after collecting reports and automatic integration receipts; one completed research is not completion of the overall goal. Unknown remote work is collected or queried before retrying it.
- Chief may improve the existing project persona and two skills from completed reports. Changes affect future snapshots; a running snapshot, its evidence and submitted result must not be rewritten or supplemented with hints to manufacture success.
- The user explicitly added chief control over initial context: configure random distribution for a research, or specify kernels and knowledge. Random sampling keeps the existing freshness policy by default; specified materials are not mixed with random draws, and per-research sampling settings do not change project defaults.
- Chief can assign a hypothesis with either context mode. The child tests that target and fills in its experimental definition; it proposes a hypothesis only when chief supplies none. Any revision preserves the original wording and status, and a supported revision cannot be reported as proof of the original hypothesis.
- Each research's manifest and seed record chief's inputs and the actual material distribution.
- Each research task uses one continuous subagent session. Test and performance analysis are skills in that same session, not separate Agents.
- The research Agent tests a supplied or self-proposed falsifiable kernel-performance hypothesis within its original continuous session.
- The Agent may submit zero, one, or many kernels.
- Every submitted kernel must already have exact-revision full-size single-kernel test evidence before final submission.
- Chief does not manually test submitted kernels and does not manually integrate versions.
- Valid final submission triggers automatic case-shape routing and version assembly.
- Hypothesis verdict, kernel performance, and integration selection are separate records.
- Mock evidence cannot prove a real hardware hypothesis.
- SSH configuration is centralized by profile reference; credentials must not be copied into prompts, logs, snapshots, reports, or repository files.
- Randomly distributed and explicitly selected kernels and knowledge are only inspiration. The Agent is encouraged to read other kernels and knowledge, and is not required to modify a supplied kernel.
- Structured knowledge must be more than one flat file.
- When a live DSH Agent behaves poorly, fix the relevant skill prompts and rerun. Do not feed hints to the running Agent, write its final answer for it, or manually repair its research result.
- Repository governance also lives in the existing chief entry skill: feature branches use `<type>/<kebab>`, `main` and `dev` accept PR merges with no other-person approval required, and creating a PR still requires the user's explicit permission. Research authorization is not PR authorization; change descriptions distinguish human design, Agent decisions and mature implementation references.

## Agent Decisions

- Initially inspected the installed DSH 0.1.0-rc.6 API, then adapted the host to the user-selected 0.1.7-alpha.2. The release choice is human design; the compatibility implementation and verification approach are Agent decisions.
- Migrated native jobs to a SessionId owner and `JobOutcome.result`, resolved optional services through `Context.get()` and preset-scoped compaction through `agentPresets.serviceFor()`, and retained the original research session and frozen snapshots.
- Kept native final-output JSON Schema within DSH's supported subset, which excludes `minLength`; a monotonic tool guard validates the prepared reference and limits research tools even when DSH registers a native subagent tool on the child's own scope after applying the inherited filter.
- Added a full official Web-profile compatibility smoke in an isolated temporary `DSH_HOME` with model steps rejected, supplementing fake-host and real ToolRuntime tests.
- Added native `meteor-skills` discovery after finding that project lookup stops at the nearest `.git` root and can miss a nested experiment project's skills. A stronger Web smoke established that chief's scoped filesystem provider also overrides a global provider regardless of rank. The correction keeps packaged defaults in a global provider at rank 600 and registers a chief-scoped provider at native `agent/created`, skipping `origin=subagent`. Current-cwd project skills use rank 99 ahead of the parent Git project's rank 100 in that same scope. The child's own frozen runtime skill layer remains authoritative.
- Kept chief's startup, bounded source research, authorized continuation and future-snapshot prompt maintenance in the existing two skills. No additional persona or chief prompt file was introduced.
- Historical startup choice: after a live chief repeatedly inspected shell/SSH setup without starting a research, the entry skill initially asked it to start immediately after reading configuration. The user's 2026-09-24 device-preparation requirement supersedes that shortcut. Chief now calls `meteor_hardware_probe(profile_ref?)`, reads the hardware report and resolves setup failures before research; it starts promptly once the report is current and ready. Historical research evidence remains unchanged.
- Implemented the requested device-preparation entry through the chief tool and CLI `meteor hardware [directory] [profile-ref]`, keeping the default execution state `unconfigured` and credentials in centralized configuration. The tool/report shape and readiness checks are Agent implementation choices supporting the human requirement.
- Empty optional profile input means automatic discovery. Chief can repair the project's own probe runner; the next probe deploys its content-hashed bundle. Initialization reuses a valid existing report, and a stable device/profile/toolchain/protocol identity keeps comparable candidates in the same integration channel while full report hashes preserve each probe's evidence.
- Kept detailed official measurement guidance in one reference under `meteor-kernel-test`, linked from the existing two skills and the developer guide. It distinguishes ACL event time from profiler task duration and counters, requires target-kernel task matching, and retains version-specific capability discovery.
- Used loopback DSH behind Tailscale Serve with an exact trusted authority and DSH's native per-authority browser-token exchange. Published access addresses stay clean, login tokens are redacted from diagnostics, and user keys/credential files are not copied into the project or smoke environment.
- Implemented the project template with Node 24 native TypeScript and Python 3.12 standard-library SQLite, with no npm runtime dependencies.
- Used one SQLite catalog per backend under `reports/meteor/<backend>/knowledge/catalog.sqlite`.
- Made JSON files projections and evidence artifacts; SQLite is authoritative for commits, novelty, and integration outbox state.
- Added `meteor_prepare_submission` validation before native structured output, with structured validation issues for repair by the original session.
- Made mock official verdicts remain `INCONCLUSIVE`; `simulated_verdict` is separate.
- Used a channel key of `backend|operator_abi|suite|environment|protocol` to serialize automatic integration.
- Added SQLite lease token and expiry for integration event claiming and token-CAS completion.
- Used content hashes for novelty idempotence so duplicate claims or kernel evidence do not refresh material freshness.
- Used `related_material_ids` to refresh existing material when a new claim provides evidence-backed progress about it.
- Made sampling read SQLite materials directly, with configured epsilon, seeded randomness, recent selection penalties over the freshness window, and active-research inflight penalties.
- Expressed chief's requested controls through optional `initial_context` and `hypothesis` inputs to `meteor_start`. Material references accept library IDs, `sqlite://kind/id`, and file/module paths. These inputs use the existing persona and two skills.
- Rendered assembled versions through the local `renderVersion` API and marked integration artifacts as `integration_validation=NOT_RUN`.
- Published remote driver bundle files atomically after writing them completely, so parallel research starts cannot read partially deployed files. The deployment phase holds no lock during kernel execution.
- Restricted integration event selection and claims to the backend, operator ABI, case suite, environment and measurement protocol of the processing project; another channel's event remains available for its original research snapshot.
- Bound final kernel evidence to the submitting research and tool-owned receipt paths, checked the linked build and current module identity, and recomputed measurement hashes. Reading other research remains available for inspiration and background evidence.

## Mature Implementation References

- **DSH 0.1.7-alpha.2**, official tag commit [`00102833dfaee1da9f48a3a8eae9d34005a75218`](https://github.com/deepseek-ai/deepseek-harness/releases/tag/dsh-v0.1.7-alpha.2), together with its installed JavaScript and declarations, defines the integration contract. This commit must not be labeled rc.6. Specific references are [jobs types](https://github.com/deepseek-ai/deepseek-harness/blob/00102833dfaee1da9f48a3a8eae9d34005a75218/packages/jobs/jobs/src/types.ts#L16-L157), [preset service lookup](https://github.com/deepseek-ai/deepseek-harness/blob/00102833dfaee1da9f48a3a8eae9d34005a75218/packages/preset/agent-preset-registry/src/index.ts#L243-L284), [spawn lifecycle](https://github.com/deepseek-ai/deepseek-harness/blob/00102833dfaee1da9f48a3a8eae9d34005a75218/packages/subagent/subagent-in-process-driver/src/index.ts#L178-L207), [scoped skills](https://github.com/deepseek-ai/deepseek-harness/blob/00102833dfaee1da9f48a3a8eae9d34005a75218/packages/skill/skill/src/index.ts#L345-L459), and [browser authentication](https://github.com/deepseek-ai/deepseek-harness/blob/00102833dfaee1da9f48a3a8eae9d34005a75218/packages/client/connection/README.md#L36-L45). These are API and behavior references, not copied implementation code.
- DSH's [jobs refactor `07941fe5e2a18abd89faa1a09f395dad164a8b66`](https://github.com/deepseek-ai/deepseek-harness/commit/07941fe5e2a18abd89faa1a09f395dad164a8b66) and [declarative preset change `d1e22a7e247060496e1acdba9ef3be1700e23ec7`](https://github.com/deepseek-ai/deepseek-harness/commit/d1e22a7e247060496e1acdba9ef3be1700e23ec7) explain the migration. The preset package is now `dsh-agent-preset-registry`; its `agentPresets` service and `serviceFor` method remain available. Where a cookbook example disagrees with the installed version's types and implementation, the latter were used.
- [`PerryLink/dsh-auto-review@7aa0569b39b3586ece543c6411e9c0604f630c0f:.github/workflows/compat.yml:L85-L131`](https://github.com/PerryLink/dsh-auto-review/blob/7aa0569b39b3586ece543c6411e9c0604f630c0f/.github/workflows/compat.yml#L85-L131) informed the full-profile smoke strategy: pin CLI and bundles to the same release, boot an isolated real profile, and distinguish loading success from model execution. Its repository was updated on 2026-09-23. Meteor independently implemented its own Web-profile checks; no community-plugin implementation was copied.
- AlphaEvolve paper `2506.13131` informed the high-level idea of diverse candidate exploration and evidence-based iterative improvement. meteor does not claim to implement AlphaEvolve.
- The remote NPU harness adaptation is based on the user’s existing workspace tools under `tools/remote_npu`, including environment bootstrap, sequence execution, batch running, and evidence collection patterns. These are treated as user workspace infrastructure, not vendor source code.
- Ascend measurement guidance uses the official [CANN 9.0 msprof arguments](https://www.hiascend.com/doc_center/source/en/CANNCommunityEdition/900/devaids/Profiling/atlasprofiling_16_0011.html), [CANN 9.0 op_summary fields](https://www.hiascend.com/doc_center/source/en/CANNCommunityEdition/900/devaids/Profiling/atlasprofiling_16_0067.html), [CANN 8.5 ACL event contract](https://www.hiascend.com/doc_center/source/zh/canncommercial/850/API/appdevgapi/aclcppdevg_03_0090.html), and [Ascend/msopprof guide](https://github.com/Ascend/msopprof/blob/master/docs/zh/user_guide/msopprof_user_guide.md), checked on 2026-09-24. These inform measurement semantics and diagnostic guidance; command examples do not assert that all upstream profiler features are exposed by Meteor. Additional versioned sources are listed in the [measurement guide](ascend-measurement-guide.md).

## Verification and Limits

- The hardware self-test follows the data flow in the pinned official [Ascend Add sample](https://gitee.com/ascend/samples/blob/166b4a5204a70b6d000be6eedc101a1238aa2df2/operator/AddTemplateCustomSample/KernelLaunch/AddKernelInvocationNeo/add_custom.cpp), adapted to fixed aligned buffers and explicit TPipe events. [CANN 9.0 synchronization](https://www.hiascend.com/document/detail/en/CANNCommunityEdition/900/API/ascendcopapi/atlasascendc_api_07_0270.html) and [scalar DataCache semantics](https://www.hiascend.com/doc_center/source/en/CANNCommunityEdition/900/programug/Ascendcopdevg/atlas_ascendc_10_00031.html) informed this implementation. Chief executes the probe, inspects its evidence, and owns project-local probe/toolchain debugging; these generic tool implementation details are Agent decisions.

- On 2026-09-23, the complete test suite passed **96/96** with no skipped tests. `tests/dsh.test.ts` passed **12/12** with the installed alpha.2 compatibility case enabled. It includes fake-host lifecycle and chief-assignment tests and a real Cordis/ToolRuntime registration/execution test.
- On 2026-09-23, `scripts/verify-dsh-web.mjs` passed using the full official alpha.2 Web profile and an isolated temporary `DSH_HOME`, with **zero model requests**. It checked chief-assigned context and hypothesis, scoped snapshot skills, the presence of native compaction, the same native child session ID, native structured output, denial of recursive delegation, job cancellation and native report delivery. This is actual native composition evidence, not a fake-host result.
- Later native Web reproductions failed when discovery used a parent Git root instead of the nested cwd, and again when a global rank-99 provider could not override chief's scoped filesystem provider. After the chief-scoped correction, the expanded real Web-profile smoke passed with `model_requests=0`: packaged skills and `meteor_init` guidance were available before initialization, current-cwd skills took priority over the parent Git project's skills, `meteor_init` refreshed discovery, and frozen child skill bodies stayed independent of project edits. The 96/96 suite and TypeScript checking also passed after this correction.
- Model/API stability and a completed research round must be verified separately before sustained autonomous operation. A smoke that blocks model requests cannot establish either.
- The smoke resolves the compaction service but does not execute a real compaction. The live UI model test has started; a completed model-authored Ascend operator and its hardware performance evidence are not claimed here.
- No vendor kernel implementation is claimed as copied into meteor.
- Mock demo success does not prove hardware performance or a real optimization hypothesis.
- Current tool capabilities come from `hardware.supported_metrics`. `kernel_time_us` represents the declared ACL event interval, not a hardware counter. `device_task_time_us` is not available unless the implemented runner advertises it; official CLI examples are manual diagnostic references whose options must be checked against the installed version.
- The dated compatibility results above describe those revisions. They do not validate later hardware-preparation and execution-proof changes, which require their own test results and actual device reports.

### 2026-09-24 chief-driven device validation

- Final local regression: **127/127 passed, no skips**, using `METEOR_DSH_MODULE_ROOT` pointed at the installed alpha.2 modules and Node's `--test-concurrency=1`. An earlier parallel run hit a Windows process-resource failure; its focused rerun and the full serial run passed. Syntax checking covered 55 source files; distribution build, TypeScript checking and `git diff --check` passed. The native alpha.2 Web-profile smoke also passed with zero model requests.

- Through the DSH alpha.2 WebUI, chief discovered the configured SSH profile and generated an immutable hardware report. Earlier failures exposed scalar-output visibility and msprof CLI compatibility defects; failed reports were preserved and generic tools were corrected.
- In the successful live run, chief independently changed the project runner from `--output <path>` to `--output=<path>` and re-probed. The resulting report was `READY`: `Ascend910_9362`, `dav-2201`, health `OK`, 20 Cube cores, 65,787,658,240 bytes total HBM; compilation, launch and all add output checks passed. The profiler matched five `meteor_hardware_add_kernel` `AI_VECTOR_CORE` tasks on the selected logical device. That CLI correction was incorporated into the distributed template and a regression fixture.
- The first subsequent research returned a valid `INCONCLUSIVE` submission with zero kernels and `research_goal_met=false`; the integration event was `SKIPPED`. This verified native report delivery and truthful empty delivery, not operator performance. Its premature stop and unsupported proposed shapes led to stricter guidance on actual case selection, executing experiments and evidence-based stopping. Historical submissions and snapshots were not rewritten.
- A later real chief supplied empty material arrays in random mode. They are now normalized as absent; nonempty conflicting material selections still fail. The start schema also names the supported budget fields explicitly. The affected initial-context and native tool tests passed **19/19** after this follow-up change.
- The last observed chief independently re-probed and obtained another READY hardware report, then launched `qmq-v1-cube-one-round`. That research failed without a valid kernel: the subagent declared source files but did not write them, then changed chief-assigned hypothesis fields and could not prepare a submission. The chief also incorrectly translated a one-research limit into a one-experiment budget. Updated guidance distinguishes these budgets and requires repairing missing source inputs within the same experiment. Build errors now identify missing files before recording an experiment; submission errors retain strict original-hypothesis validation and show differing expected/actual fields. Regression tests cover those repairs. Their effect on a future model-authored kernel remains unverified; no operator performance result or integrated version is claimed for this run.

### 2026-09-24 continued WebUI validation and annotation proposal

- Four further WebUI research runs, including one initialized from an empty directory, independently obtained real READY hardware reports but returned no validated qmq-v1 kernel. The final run retained the default 12-experiment budget, wrote device/host sources, and stopped at manifest validation. See the [dated follow-up report](2026-09-24-live-research-followup.md) for observed outcomes and limitations.
- Agent implementation fixes cover chief file reads, equivalent current-research write paths, actionable missing manifest/source/dependency errors, and compiler-log diagnostics. The published build also exposed an invalid contract-document fallback after TypeScript type erasure; each new research now receives an actual readable module-contract document. Launcher errors state expected and actual symbols. These changes retain immutable snapshots, write boundaries and strict evidence acceptance.
- Fresh local verification passed **133/133 tests with no skips**, syntax checking of 55 source files and TypeScript checking. Native Web composition checks also exercise the generated contract reference; live model success remains a separate unfulfilled validation.
- The user subsequently requested structured source annotations for cost/parallelism analysis and automatic performance evidence. The [design proposal](plans/2026-09-24-structured-performance-annotations.md) separates that human request from Agent choices, cites official vendor/compiler tools, and preserves exact-source evidence through generated annotated views. Its new annotation/plan/evidence functionality is a proposal, not implemented or hardware-validated capability.

### 2026-09-24 delegation and authoring follow-up

- Human direction prioritizes successful DSH operator delivery and makes starting/managing research subagents Chief's main responsibility. Entry guidance now starts a ready-device research promptly with a goal; hypothesis and material selection remain optional Chief controls. The structured-annotation implementation remains deferred.
- Agent choices: accept raw kernel sources as hashed inspiration; make explicit selection authoritative over irrelevant valid sampling fields and report that normalization; expose file-read truncation/continuation offsets; reject invented case IDs before spending an experiment. These changes preserve exact specified materials and evidence validation.
- The fresh WebUI run independently initialized/probed and dispatched at step 8, then Chief tried to continue after an empty submission. Candidate compilation and repeated specified-mode argument failures remain recorded, as detailed in the [follow-up report](2026-09-24-live-research-followup.md). Neither this dispatch result nor an INCONCLUSIVE report proves a working operator.
- Authoring guidance references official CANN compilation constraints, scalar conversion and memory semantics, with precise versioned links in [Ascend authoring](../templates/project/.dsh/skills/meteor-kernel-test/references/ascend-authoring.md). No candidate implementation is supplied by the test operator, and no running snapshot or historical evidence is rewritten.
- Local installed DSH 0.1.7-alpha.2 and pi-ai 0.85.1 inspection found that the Responses path defaults to `strict:false`; it does not establish that local schema conversion forced the Chief's repeated sampling arguments. The compatibility repair addresses the observed call shape without attributing unverified behavior to the upstream API.
- Further live traces showed voluntary final submission with feasible repairs and budget remaining. Installed DSH alpha.2 has no default step/turn cap for this spawn; native structured output concludes the run. Agent choices separate evidence verdict from stopping conditions in the existing persona, startup package and submission descriptions, without a minimum experiment count or a programmatic scientific-verdict gate. The same guidance permits probe debugging while retaining full tests for every delivered revision.
- Additional official authoring references cover the CANN 9.0 SIMD launch ABI and DataCache writeback/shared-line counterexamples. These are general diagnostic guidance, not fixes supplied to a live candidate; the new prompts apply only to future research snapshots.
- Agent tool repair: preserve bounded numerical verification diagnostics in failed test rows instead of dropping them; include up to eight actual/expected yScale mismatches with strict-JSON nonfinite values. Finite-only error fields retain their meaning, malformed output retains bounded fallback details, and failed timings remain ineligible. Accuracy thresholds and original receipts are unchanged; the new verifier hash participates in new suite/oracle identities. Full regression passed **142/142**, with no skipped tests; syntax and TypeScript checks passed.
- Final live WebUI acceptance in a fresh `persistent-research` workspace completed the requested delivery: Chief dispatched at step 7; the same native child authored and tested `qmq_fused_scalar@r2`, passed all four pinned real-device cases with candidate-specific AI_VECTOR_CORE witnesses, and formally submitted one kernel. Automatic integration returned ASSEMBLED with four shape rules, then Chief read the evidence and completed its goal. Details and exact receipt IDs are in the [follow-up report](2026-09-24-live-research-followup.md). This proves baseline delivery, not acceleration or correctness outside the tested cases; version integration tests remain intentionally NOT_RUN.
- A small follow-up preserves null failure reasons as absent and documents that fixture arguments are only for explicit mock tests. Interface/SSH tests passed **28/28**. The live acceptance used the preceding immutable research snapshot; its measurements and final submission were not rewritten.
